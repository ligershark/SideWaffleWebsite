// Release Notes

var version = $("ul");
var release = $("li.release");

$.each(version, function (i, val) {
    //$(i).addClass("list-group");
});

$.each(release, function (i, val) {
    //$(i).addClass("list-group-item");
});